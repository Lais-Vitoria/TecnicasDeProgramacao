#include <stdio.h>
#include<string.h>
#include <stdbool.h>

#define tamanho_max_caracter 100

int main()
{
    char str[tamanho_max_caracter];
    char caractere_busca;

    printf("Digite um caracter para buscar em '%s': ", str);
    if (scanf(" %C", &caractere_busca) != 1)
    {
        
    }
    
}